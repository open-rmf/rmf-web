{
  "name": "poetry-install",
  "version": "0.0.0",
  "description": "A dummy package so that we can run poetry install only when needed",
  "private": true,
  "scripts": {
    "install": "../scripts/poetry-install.sh"
  }
}
