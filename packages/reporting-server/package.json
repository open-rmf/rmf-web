{
  "name": "reporting-server",
  "version": "0.0.1",
  "description": "dummy package",
  "private": true,
  "scripts": {
    "prepack": "poetry run python setup.py bdist_wheel",
    "start": "poetry run python -m reporting_server",
    "test": "python3 -m poetry run python -m unittest",
    "test:cov": "RMF_API_SERVER_LOG_LEVEL=CRITICAL RMF_API_SERVER_TEST_COVERAGE=1 poetry run python -m coverage run -p -m unittest && poetry run python -m coverage combine",
    "test:report": "poetry run python -m coverage html && xdg-open htmlcov/index.html",
    "lint": "python3 -m poetry run pylint rest_server"
  },
  "devDependencies": {
    "poetry-install": "file:../../poetry-install"
  }
}
