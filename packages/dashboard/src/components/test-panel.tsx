import { Typography } from '@material-ui/core';
import React from 'react';

//To be removed

export const TestBuildingPanel = (): React.ReactElement => {
  return <Typography variant="h5">This is a new Tab Content for BUILDING MANAGEMENT</Typography>;
};

export const TestRobotsPanel = (): React.ReactElement => {
  return <Typography variant="h5">This is a new Tab Content for ROBOTS</Typography>;
};

export const TestTasksPanel = (): React.ReactElement => {
  return <Typography variant="h5">This is a new Tab Content for TASKS</Typography>;
};

export const TestLogsPanel = (): React.ReactElement => {
  return <Typography variant="h5">This is a new Tab Content for 7-DAY LOGS</Typography>;
};
