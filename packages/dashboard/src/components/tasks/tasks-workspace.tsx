import { WorkspaceState } from '../workspace';

export const tasksWorkspace: WorkspaceState = {
  layout: [{ i: 'tasks', x: 2, y: 0, w: 8, h: 12 }],
  windows: [{ key: 'tasks', appName: 'Tasks' }],
};
