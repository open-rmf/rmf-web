config = {
    "root_path": "/api",  # base path to mount the app on, must not end with '/'
    "host": "localhost",  # ip or hostname to bind the socket to
    "port": 8001,
    "api_server_url": "http://localhost:8000/socket.io",
}
