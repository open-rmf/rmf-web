{
  "name": "api-client",
  "version": "0.0.1",
  "description": "",
  "main": "dist/index.js",
  "private": true,
  "scripts": {
    "prepare": "tsc --build",
    "build": "npm run lint && tsc --build",
    "lint": "../../node_modules/.bin/eslint --ext ts,tsx lib"
  },
  "author": "koonpeng@openrobotics.org",
  "license": "Apache-2.0",
  "dependencies": {
    "axios": "^0.21.1",
    "socket.io-client": "^3.1.2"
  },
  "devDependencies": {
    "typescript": "^4.2.2"
  },
  "files": [
    "dist/"
  ]
}
